name: loreon workflow

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps: 
            - name: checkout Repo
              uses: actions/checkout@v5

            - name: welcome message
              run: echo "my first workflow on github"

            - name: List Files
              run: ls

            - name: Read file
              run: cat README.md