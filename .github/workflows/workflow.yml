name: loreon workflow.  #workflow name 

on: push

jobs:                   #jobs: task you want to acomplish 
    first_job:
        runs-on: ubuntu-latest
        steps:                        #steps: actions that help complete a task 
            - name: checkout Repo
              uses: actions/checkout@v5

            - name: welcome message
              run: echo "my first workflow on github"

            - name: List Files
              run: ls

            - name: Read file
              run: cat README.md